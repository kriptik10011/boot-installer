# Environment Configuration Example
# Copy to .env.development for local development overrides

# API Configuration
# Default port is 8000 (used in production)
# Override during development if needed
VITE_API_PORT=8000
VITE_API_HOST=localhost
